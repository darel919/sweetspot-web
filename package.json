{
  "name": "sweetspot-web",
  "dependencies": {
    "nuxt": "^4.5.2",
    "vue": "^3.5.41",
    "vue-router": "^5.2.0"
  },
  "private": true,
  "scripts": {
    "dev": "bun run generate:profile-index && nuxt dev",
    "generate": "bun run generate:profile-index && nuxt generate",
    "generate:profile-index": "bun scripts/generate-calibration-profile-index.mjs",
    "deploy": "bun run generate && bunx wrangler deploy",
    "test": "bun test"
  },
  "type": "module",
  "devDependencies": {
    "typescript": "5.9.3",
    "vue-tsc": "^3.3.11"
  }
}
